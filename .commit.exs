# Used by "mix commit"
[
  required: [:footer, :body, :scope], # specifies the parts of a commit that must be included
  footer_fields: [:author, :reviewed_by, :refs] # specifies the required footer fields.
]
